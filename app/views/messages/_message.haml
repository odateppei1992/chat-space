.message
  %span.user_name
    = message.user.name
  %span.created_at
    = message.created_at.strftime('%Y/%m/%d %H:%M')
  .body
    = message.body
  .message_image
    = image_tag message.image if message.image.present?
